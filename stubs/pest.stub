<?php

describe('Success Cases', function () {
    it('works', function () {
        $user = userFactory()->create();
    });
});

describe('Error Cases', function () {
    it('does not works', function () {
        $user = userFactory()->create();
    });
});

/**
 * ? ***********************************************************************
 * ? Helper Functions
 * ? ***********************************************************************
 */
